-- AVG() en SQL es otra una funcion de agregación que se utiliza para calcular el promedio (la media aritmética) de los valores numéricos (es la abreviatura de average)

SELECT AVG(AGE) FROM users <-- me devolvera el promedio entre todas las filas de la columna edad.