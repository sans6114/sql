-- El comando WHERE en SQL se usa para filtrar las filas que se obtienen de una tabla, basándose en una o más condiciones específicas.
SELECT * FROM users WHERE age = 19; <-- retorna solo las filas de mi tabla users donde la columna "age" sea igual a 19.
