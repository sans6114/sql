-- ORDER BY en SQL se usa para ordenar los resultados de una consulta en un orden específico. 
SELECT * FROM users ORDER BY age; <-- me devolvera las filas de mi tabla pero ordenado de forma ascendente por campo "age"
SELECT * FROM users ORDER BY age DESC; <-- me devolvera de forma descentende por campo edad, por defecto esta el "ASC"
