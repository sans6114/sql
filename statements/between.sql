-- BETWEEN en SQL es un operador que se utiliza en la cláusula WHERE para seleccionar valores dentro de un rango específico. Este rango incluye tanto el valor inicial como el valor final que defines.

SELECT * FROM users WHERE age BETWEEN 10 AND 30 <-- me devuelve los usuarios donde la edad (campo age) este entre 10 y 30